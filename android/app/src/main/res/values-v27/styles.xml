<?xml version="1.0" encoding="utf-8"?>
<!--
  Display-cutout handling. Split into its own API-27 overlay because
  android:windowLayoutInDisplayCutoutMode did not exist before Android 8.1 and
  this project is minSdk 24 — declaring it in values/styles.xml would be a
  resource-linking error rather than a graceful ignore.

  shortEdges lets the window extend into the notch area instead of the system
  letterboxing around it, which is what edge-to-edge means on a cutout device.

  IT IS ONLY SAFE BECAUSE THE LAYOUT READS THE SIDE INSETS. Until 2026-08-16
  nothing in the web layer touched safe-area-inset-left/right — the native
  shell padded them silently — so a cutout in landscape would have sat on top
  of the call controls. The <main> in src/routes/_authenticated/app.tsx now
  pads for both, and that pairing is the whole reason this line can exist.
-->
<resources>
    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>
</resources>
