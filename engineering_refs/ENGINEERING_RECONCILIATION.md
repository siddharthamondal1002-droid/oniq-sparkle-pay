# ENGINEERING RECONCILIATION — atlas & 1000-detail loops vs measured ONIQ evidence (2026-08-23)

MEASURED ONIQ EVIDENCE WINS. Preserved unchanged: aliveness >= 0.75 · knee
damping x0.50 (BVH layer, six channels, symmetric) · padding contract ·
rig-confidence gate (kpt mean >= 0.70) · mesh-density policy (GRID40
default; 60/80 thin-SOLID-limb candidates) · thin-limb vs thin-stroke
separation · thin-stroke STRESS_FAIL · fail-closed STILL_PARALLAX +
MOTION_CONTRACT fallback · per-host/per-CPU-class determinism (verified
again this loop: regression renders byte-matched the recorded this-host
hashes a60f80b7/757228fd/894d29b1; original-host set intentionally NOT
matched and NOT re-baselined) · mandatory pixel review.

Conflicts recorded, not silently resolved:
1. ENG-004 atlas offers profile/rear/3-4/back camera panels; the measured
   character-motion envelope is FRONTAL/NEAR-FRONTAL only. Resolution:
   those panels are scene/reference vocabulary; combination records carry
   support=blocked-for-character-motion. Envelope not broadened.
2. ENG-004 special effects (lens flare, volumetric, god rays, bloom) and
   S2 weather panels use vocabulary that can trigger the one-authoritative-
   weather classifier; lighting words in the ENFORCED register must remain
   vfx/ambience-invisible (pinned by test). Atlas entries carry an explicit
   vfx_caution flag on weather rows.
3. ENG-004 Kelvin scale and lighting-ratio language: REFERENCE_ONLY; the
   pipeline has no color-temperature calibration. NOT_SPECIFIED recorded
   wherever the atlas gives no number.
4. Prior recorded conflicts stand (ENG-002 0.70-vs-0.75; width-only limb
   classes; DOF tables; cm foot thresholds) — see engineeringReference.ts
   DISCREPANCIES, pinned by test.
5. ENG-003's printed "1,000 variables" and ENG-004's printed "10,000
   details" were NOT treated as measured facts; controlled sets were
   constructed instead (1,000 authored details; 5,000+5,000 programmatic
   combinations + 10,000-pair matrix), every record traceable to its
   source panel and marked with an honest evidence class.
